#!/bin/sh
# Run tests with the same Python resolver as the aifilm CLI.
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON=$("$SCRIPT_DIR/runtime-python")

exec "$PYTHON" -m pytest "$@"
