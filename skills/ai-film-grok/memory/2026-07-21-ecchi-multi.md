# Memory · 成人尺度 + 女主弹性（2026-07-21）

**用户**：尺度拉高；单女主与多女主都要，取决于 Prompt 与输入图。

## 默认（弹性 · 少硬编码）

- **不自动钉** heat_scale=max；比例仅 advisory / 极端 soft warn
- **女主默认 single**；multi 仅证据成立
- 工程门禁硬，叙事尺度软

## 入口

- `references/ecchi-story.md` §女主弹性
- `edit_policy.resolve_heroine_cast_mode` / `lint_multi_heroine(active=…)`
