# Memory · 2026-08-04 · 用户命令是圣旨

**制度入口**：`~/.grok/Agents.md` → 圣旨协议 · plugin `Agents.md` 一行指针

## 用户原话
> 帮我打通 以后我的命令是圣旨 写入记忆

## 三句话
1. **用户短令 = 圣旨**：`go` / `go next` / `ok go` / `推进` / `打通` 等 → **立刻执行**既定 next，禁止重开讨论、禁止连环「要不要…」。
2. **上一轮已列的下一步**，用户 go 后整条链做完（验→commit→update→点名则 push），中途不停等「继续」。
3. **「写入记忆/制度」当轮落档**；不可逆仍一句话计划，但圣旨已点名该步 = 已确认。

## 检查清单
- [x] `~/.grok/Agents.md` 写入「圣旨协议」（改前已 `cp` 到 `~/.grok/backups/`）
- [x] 本 memory 短卡落档
- [x] plugin `Agents.md` 指针一行
- [ ] 新会话抽样：用户 `go next` 时 agent 不重问是否 push/commit

## 链
- 全局：`~/.grok/Agents.md` 圣旨协议
- 出货纪律：`memory/2026-07-29-agent-ship-discipline-iron.md`
- 短令旧约：Agents 语言与沟通 · 短指令惯例
