#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
PUBLISH_SCRIPT="/Users/dex/.agents/skills/gitea-publish/scripts/gitea_publish.py"
PUBLISH_HOOKS="/Users/dex/.agents/skills/gitea-publish/hooks"

GIT_CONFIG_COUNT=1 \
GIT_CONFIG_KEY_0=core.hooksPath \
GIT_CONFIG_VALUE_0="$PUBLISH_HOOKS" \
python3 "$PUBLISH_SCRIPT" hook pre-commit --repo "$ROOT" --json
