#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$ROOT"

if ! python3 scripts/sync_project_docs.py --check; then
  echo "pre-push blocked: generated project docs are stale; run 'make sync-docs' then retry." >&2
  exit 1
fi

make release-check
