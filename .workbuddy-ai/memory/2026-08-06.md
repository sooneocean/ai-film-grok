# 2026-08-06

## Round 6 — 闭合开放缺口 + 数据自描述（A→B→C→D 全做）

侦察审计发现：
- 70 缺口 = 60 open(fill) + 10 routed_generate(generate/acep-step)。
- **60 个 open fill 缺口全部 `suggested_asset_id` 指向 approved 资产**，但 `generate_loop.do_submit` 只处理 action==generate 缺口 → 无任何自动化闭环（只能人手 fill_gap.py）。
- 10 个 generate 缺口全部缺 `duration` 字段（_build_spec 只能从 to-generate.jsonl 兜底）。

交付（实时仓库仅 dry-run，未 --apply/--fix）：
- **A** 新增 `tools/fill_open_gaps.py`：默认 --dry-run，--apply 才写盘；`eligible(gaps,cat)` 纯函数分可闭合/死路；逐条委托 fill_gap.py 闭环；--limit 限量；可重入。
- **B** `reconcile.py --fix` 按 source_gap_id 从 to-generate.jsonl 回填 generate 缺口 duration(30s)；`generate_loop._build_spec` 优先用 gap.duration；`route.py check` 加软 WARN（generate 缺 duration 不破 gate）。
- **C** `report.py` 加 open fill 可闭合/死路 + generate 缺 duration 计数；`reconcile.py` 审计加同类项。
- **D** `tools/tests/test_fill_open_gaps.py`（8 用例，纯函数+main dry-run 契约）；`self_test.py` 扩展：reconcile --fix 回填验证、注入 3 fill+1 死路断言 --apply 闭环3/留1/rev+3/use_count+3、--dry-run 不写。

验证：run_tests.py **54 测试全过**（46+8）；self_test **PASS**；实时仓库 pristine（git 现状为前几轮遗留改动，本轮仅新增 fill_open_gaps.py + tests/，命令全只读）。

应用入口（留待确认）：
1. `python3 tools/reconcile.py --fix`（回填 10 generate 缺口 duration，低危可逆）
2. `python3 tools/fill_open_gaps.py --apply`（闭环 60 fill 缺口，rev 92→152、use_count 起跳；每条有 .bak 备份）

## 已执行（用户 "GO" 后 09:52 应用）
- 已做安全备份：`/tmp/catalog.preround6.json`、`/tmp/gap-queue.preround6.jsonl`。
- `reconcile --fix`：10 generate 缺口 duration 全部回填 30.0（catalog 未改动，仅 gap-queue 存 .bak）。
- `fill_open_gaps --apply`：**60 fill 缺口全部闭环**（rev 92→152；仅 2 个资产被 60 次复用，use_count 起跳）。
- 后验：`route.py check` 0 警告 OK；`reconcile` 审计 0 缺 duration / 0 open fill  backlog；`report` 显示 60 filled + 10 routed_generate。
- 残余：10 acestep job 仍 submitted（等 5090 ACE-Step 落 wav，外部依赖）。git 改动：catalog.json + gap-queue.jsonl 已 M；.bak 被 gitignore。

## git commit
- `git commit` → `c8bd118` "feat: closed-loop pipeline automation (Round 5 + 6)"，42 files / +4025 / -143。含 Round5+6 工具、Round6 数据闭环、RUNBOOK、单测、memory。pre-commit 门禁（validate_catalog --no-sha）通过。working tree 干净。未 push（用户未要求）。

## Round 7 — 覆盖驱动的前瞻供给规划 + 统一编排入口（A→B→C→D 全做）

用户「go next」→ 重审计发现库存**严重偏科**：rnb-full 缺口 59× 仅 2 资产；ambient 库存 0（已转 10 生成缺口但待落盘）。之前只有「缺口来了才生成」的被动模式，缺少「按供需缺口前瞻排产」能力。经确认全做 A→B→C→D。

### A — 供需覆盖分析器 (`coverage.py` 新)
- 分析粒度 `(mood, stem, energy_bucket)`，`energy_bucket(e)`：`low<0.34 / mid<0.67 / high`。
- 供应 = approved 资产数；在制 = routed_generate 缺口（算半供应）；需求 = 该粒度下全部缺口数；`effective=approved+inflight`；`deficit=max(demand,target_min)-effective`（`target_min` 默认 4）。
- 分类：`STARVED`(approved=0 且 inflight=0) / `THIN`(effective<demand 或 <target_min) / `OK`。
- 纯函数 `analyze(cat,gaps,target_min)` / `priority_queue(analysis,max_total=30,per_cap=10)`（deficit 降序、按 (mood,stem) 封顶、总量封顶）。
- `--emit-generate [--apply]`：把 Top-N 落成 open `generate` 缺口 + 追加 `to-generate.jsonl` 台账（默认 dry-run；落库带 `.bak`）。缺口字段：`reason="coverage_gap"`、`dedup_key="coverage|mood|stem|bucket"`、`action="generate"`、`status="open"`、`suggested_asset_id=None`。
- `--json` 机读（top keys: target_min/rows/supply_total/demand_total/priority）。

### B — report.py 集成覆盖摘要
- `report.py` 新增 `from coverage import analyze` 调用，输出 library coverage STARVED/THIN 摘要（`try/except` 包裹，离线不影响其他行）。

### C — 统一编排入口 (`pipeline.py` 新)
- dispatcher 把 ~20 脚本收口为子命令：`check/doctor/submit/poll/run-acestep/fill-open/reconcile/coverage/one-shot/status`，各子命令经 `subprocess.run([sys.executable, HERE/script] + extra)` 委托现有已测工具（行为不变，单点入口）。
- `one-shot`：`fill-open`→`reconcile --fix`→`submit`→`run-acestep`→`poll --auto-approve`（或 `--demo` 走 MockBackend 端到端）→`route check`→`reconcile`。
- `status --json`：汇 `{catalog_revision, assets, gaps_by_status, coverage:{target_min,starved,thin}, jobs}` 为健康 blob。

### D — 测试 (`tests/test_coverage.py` 新) + self_test 加固
- `AnalyzeTest`：`test_classify_ok_thin_starved_and_inflight`（energy 0.2→low 桶）、`test_priority_queue_orders_by_deficit_and_caps`（deficit 降序 + per_cap/max_total 封顶）。
- `EmitTest`：`test_dry_run_writes_nothing`、`test_apply_writes_gaps_and_togen_with_backup`（验证 reason/action/status/None suggested + `.bak` 文件）。
- `self_test.py`：demo run 改为 `tools/pipeline.py one-shot --demo`；加 `pipeline.py check` 断言；修 `gen_filled` 按 `gap_id` 追踪（fill_gap 重写 action→"fill" 导致旧 `action=="generate"` 过滤漏掉 10 generated 缺口；`gen_ids` 在 demo 前捕获，解决「60 预填使 filled==expect(10) 断」问题）。

### 验证汇总（全绿）
- `tools/run_tests.py` → **58 测试 OK**（原 54 + test_coverage 4）。
- `tools/self_test.py` → **SELF-TEST PASS ✓**（pipeline.py one-shot --demo 端到端 + Round6 fill-open/duration 验证 + TTS 双管线分离）。
- 实时仓库只读演示：`coverage` 22 粒度 → OK 2 / THIN 19 / **STARVED 1**（`rnb/full/mid`：approved 0、inflight 0、demand 27、deficit 27）；supply 43 / demand 70 / target_min 4；`coverage --emit-generate` dry-run 优先级队列 14 条；`pipeline.py status --json`（rev 152、60 filled + 10 routed_generate、coverage starved 1/thin 19、10 acestep:submitted jobs）。
- 实时仓库 pristine：git 仅 `M tools/report.py` / `M tools/self_test.py` / `?? coverage.py` / `?? pipeline.py` / `?? tests/test_coverage.py` + memory；catalog/gap-queue 数据未动。

### 应用入口（留待确认，非被动缺口）
- `python3 tools/coverage.py --emit-generate --apply`：前瞻排产 ~14 个 generate 缺口（补齐 rnb 偏科 + 其他 THIN），需 acestep/LTX/Grok 后端真正产出音频才能闭合（外部依赖未变）。
- 本轮未 commit（用户未要求，沿用 Round6 「接上再commit」先例）。

### 已应用（用户「gO」后 10:21）
- 安全备份：`/tmp/gap-queue.preround7.jsonl`、`/tmp/to-generate.preround7.jsonl`、`/tmp/catalog.preround7.json`（catalog 未改）；工具落库另带 `.bak`。
- `coverage.py --emit-generate --apply`：**30 generate 缺口 + 30 to-generate.jsonl 条目**入队（优先级队列 deficit 累加至 max_total=30 封顶，跨 14 个 (mood,stem,bucket) 粒度；rnb/full/mid 补 4、rnb/full/high 补 2 等）。缺口字段 status=open/action=generate/reason=coverage_gap/suggested_asset_id=None，均带 dedup_key。
- `reconcile --fix`：按 source_gap_id 从 to-generate.jsonl 回填 30 个新缺口 duration=30.0（消除 route check 软 WARN）。
- 后验：`route.py check` 0 警告 OK；`reconcile` errors=0；**残余 6 WARN = 6 个 open generate 缺口「no capable backend」死路**（dark/playful/sensual/warm 中部分 mood×stem 不在任何 active 后端 capabilities 内）——真实供需能力缺口，非数据缺陷，待后端扩 capabilities 或新增后端后可填。
- 实时仓库现状：gaps 70→**100**（filled 60 / routed_generate 10 / open 30，其中 6 死路）；togen 10→40；catalog 未动（rev 152 / 46 资产）。`run_tests.py` → 58 OK。
- 未 commit（用户「gO」仅指应用 emit，未含 commit）。

## Round 7 后续 — 解锁 6 死路缺口 + acestep 真实命令推进（用户「推进全部」10:28）

### 根因定位（item ①）
- 6 个 no-capable-backend 死路缺口全部是 `stem_profile="pulse"`（energy 0.8 → high 桶）：dark/pulse×2、playful/pulse×2、rnb/pulse×2。
- 原因：active 后端 acestep/ltx23 的 `capabilities.stem_profiles` 只声明 [pad, full, thin]，漏了库内真实存在的 `pulse` 茎型（coverage 按数据里的茎型分桶，故 pulse 需求被排产却无后端认领）。
- 修复：`generators.json` 给 acestep + ltx23 的 stem_profiles 补 "pulse"；`updated_at`→2026-08-06T10:30Z；invocation.cmd 占位提示改为自描述（保留 `REPLACE_ME` 守卫 + 列出 {seed}{mood}{stem}{duration}{out}{job_id} 占位）。

### 路由（generate_loop --submit，/tmp 备份）
- 加 capability 后死路=0（pulse 全部路由到 acestep，local 优先）。
- submit 同时触发管线标准 eligibility 捷径 `find_existing_candidate`：30 个 coverage_gap open 缺口中 **20 个被现有 approved 资产复用填充**（同 mood/stem/energy 匹配，避免重复生成），**10 个路由到 acestep 待生成**（6 pulse + 4 其余无匹配）。
- 结果：gaps 100→**100**（filled 60→80、routed_generate 10→20、open 30→0）；catalog rev 152→**172**（20 次 fill 各 bump）；generation-jobs 10→**20**（全 acestep/submitted）。
- reconcile 后验：errors=0 / warnings=0（死路清零）/ info=20（在制工单）。route check 0 警告。

### item ② acestep 真实命令（仍待用户提供）
- `run_acestep.py` 守卫 `"REPLACE_ME" in cmd` → 占位命令会被跳过，不执行（不会落垃圾文件）。
- `invocation.cmd` 经 `backends/acestep.py` 用 `.format(seed, mood, stem, duration, out, job_id)` 渲染；`out`=`bgm-library/pending/{job_id}.wav`（watch_dir+out_pattern）。
- 待用户提供：**5090 节点上真实 ACE-Step 调用命令**（保留上述占位变量）。配好后 `run_acestep.py --pending` 落 wav → `generate_loop.py --poll --auto-approve` 闭合 20 工单（10 ambient + 10 coverage）。
- 本次进展（capabilities + submit 数据态）已提交（commit 见对话/ git log）。

## 工作流优化诊断 — 用户的真实生成手段是视频，仓库目前只有音频层
- 用户主手段：**Grok OAuth Video 1.5（云端）** + **本地 5090 H3（i2v / r2v / t2v）**。但 `tools/` + `bgm-library/` + `tts-evaluations/` 全是针对**音频/BGM/TTS** 的闭环管线，视频侧完全空白。
- 配置错位：`generators.json` 里 `grok15` 被配成音频源（`api.x.ai/v1/audio`，role TBD），`h3` 是 pending 的「声音源」——两者其实都应是**视频后端**。
- 复用点（无需重写）：整条音频闭环（gap→route→backend→generate→ingest→QA→review→fill）已被 `asset_kind`(bgm/tts) 泛化证明可行；`ApiBackend`(云端 Bearer/OAuth + poll/download) 可直接改造成 `VideoApiBackend`；`AceStepLocalBackend`(5090 脚本回流) 可镜像成 `H3LocalBackend`；`router.capable_backends` 能力矩阵、`breaker`、`coverage`、`qa_audio` 硬门禁/软告警结构、`pipeline.py` 编排、`validate_*`/`reconcile`/`report`/`self_test` 全可复用。
- 最值钱的桥梁：资产 schema 已有 `use_count`/`last_used_film_id` 钩子 —— 下一步应是 `assemble.py`：按 scene 选 approved 视频 + 匹配 BGM(按 mood/energy) + TTS 配音 → ffmpeg 合成最终片段，并产出 `film_manifest.json` 时间线，让整片可复现/版本化。
- 新依赖提示：音频是纯 stdlib，视频 QA/转码需 ffmpeg（唯一新增外部依赖）。Grok Video 1.5 真实 API 契约、H3 CLI 命令仍待用户确认。
- 已向用户交付分阶段 TODO plan（P0 修配置 → P1 视频后端 → P2 数据层 → P3 视频QA → P4 闭环/coverage → P5 编排/可观测 → P6 场景装配桥梁 → P7 收尾）。未实施，等用户选起点。

## 视频镜头闭环流水线 P0→P7 全量推进（已完工）

用户选「全量推进 P0→P7」。把音频闭环镜像成**第三条车道 video**，接入真实栈：
`generators.json` 里 `grok-video15`(video_api, OAuth `XAI_API_KEY`, 支持 t2v/i2v/r2v) 与
`h3`(video_local, 5090 H3, t2v/i2v/r2v) 均 `active`。

### P0 配置 + 路由
- `router.py`/`video_route.py`：capability 矩阵按 `asset_kind:["video"]` + `mode`(t2v/i2v/r2v) + `resolution` + local-first 过滤；`find_existing_video_candidate` 资格捷径（同 mood/mode/energy 或近重复 → 直接 fill 而非重生）。
- `generators.json` 确认 `grok-video15`/`h3` 即用户真实栈；`grok15` 音频配置不改（保持原样，视频走这两个专用条目）。

### P1 视频后端
- `backends/video_api.py`(`VideoApiBackend`：submit 上传 prompt/参考图→ext_id→poll→下载 mp4 到 pending/；Bearer/OAuth、重试/退避)、`backends/h3_local.py`(`H3LocalBackend`：写工单到 `video-library/.gen-tickets/*.json`，operator 5090 跑完落 pending/)、`backends/mock_video.py`(测试)。`backends/__init__` 注册 `kind: video_api / video_local`。

### P2 数据层
- `video_pipeline_lib.py`：load_vcatalog/load_vgaps/load_vjobs + 路径(root=video-library)；env override `AIFILM_VIDEO_LIB`（隔离测试，与 `AIFILM_BGM_LIB` 并列）。
- `validate_video_catalog.py`：schema `aifilm-video-library-v1` / 资产 `aifilm-video-asset-v1`；写前 gate。

### P3 视频 QA
- `qa_video.py`：ffmpeg `signalstats`(YAVG, limited luma 黑=16) 算 black_frame_ratio + 帧间 frozen_score。
  **硬门禁**（不达标不自动通过）：可解码(有视频流)、duration≥0.5s 且在请求±15% 内、黑场≤5%、冻结>0.02。
  **软告警**：分辨率档不符、自带音轨、near_dup(cosine≥0.98)。ffmpeg 缺失→降级为「仅可解码+时长」并告警。

### P4 闭环
- `ingest_video.py`(重算 sha256/指纹/QA/入 catalog，`--normalize`)、`generate_loop_video.py`(submit→poll→ingest→auto-approve+fill；--demo 走 MockVideoBackend；--no-qa-gate 覆盖；失败回退 gap→open)、`coverage_video.py`(STARVED/THIN/OK + priority_queue，--emit-generate/--apply)、`fill_gap_video.py`(修 `import sys` bug，之前导致自动 approve 后缺口卡 routed_generate)、`reconcile_video.py`(孤儿/stuck 复位/指纹回填，--fix/--strict)。
- 关键 bug 修复：P4 demo 缺口卡死因 `fill_gap_video.py` 用 `sys.path.insert` 却未 `import sys` —— 补 `import sys` 后闭环正常，`use_count` 正确累加。

### P5 编排 + 可观测
- `video_pipeline.py`（镜像 `pipeline.py`）：check/submit/poll/run-h3/reconcile/coverage/one-shot/status/assemble；`run-h3` 打印 5090 待跑工单（跳过已产出、告警 REPLACE_ME）。
- `report.py` 加 `_report_video()`（按状态/模式/缺口/覆盖/工单）；`gen_review.py` 加 `video-library/review/index.html`(`<video>` 卡片 + 批准/拒绝命令 + 孤儿告警)；`reject_asset_video.py`(pending_human_review→rejected/)；`tests/test_video.py`(12 例：qa/validate/coverage/router)。

### P6 场景装配桥梁
- `assemble.py`：ffmpeg `filter_complex` 合成 approved 视频 + 匹配 BGM(同 mood+energy_bucket，0.35 压低) + 可选 TTS(1.0)；`auto_manifest` 写 `films/film_manifest.json`(含 sha256)；单视频→`volume=0.35,apad`，多音轨→`amix`。`tests/test_assemble.py`(6 例，含真 ffmpeg 合成 e2e 验证 video+audio 流)。
- `pipeline_lib.py` 加 `_env` + `AIFILM_VIDEO_LIB` 覆盖；`video_pipeline.py` 加 `assemble` 子命令。

### P7 收尾
- `.git/hooks/pre-commit` 现在**同时**校验 `bgm-library/catalog.json`(`validate_catalog.py --no-sha`) 与 `video-library/catalog.json`(`validate_video_catalog.py --no-sha`)，双车道同款门禁。
- 新建 `RUNBOOK-video.md`：镜像 `RUNBOOK.md`，覆盖 Step0 配 generators(grok-video15 OAuth + h3 5090) → Step1 submit → Step2 跑后端(本地 H3 via run-h3 / 云端 Grok Video 1.5) → Step3 poll --auto-approve + QA 硬/软门禁 → Step4 验证 → Step5 assemble → 运维要点(熔断/缺口回退/stuck/资格短路/车道隔离/ffmpeg 单依赖) → 不变量(schema/媒体不进 git/后端=加条目/三车道共享 generators 按 asset_kind 隔离)。

### 回归结果（收尾验证）
- `tools/run_tests.py`：**76 测试全过**（OK，2.3s）。
- `tools/self_test.py`：**PASS** —— 闭环 + TTS 双管线 + Round6 fill-open/duration + 视频车道 e2e(3 gaps t2v/i2v/r2v → 3 assets/3 approved/3 filled，bgm catalog 0 泄漏，tts 缺口仍 open 验证车道隔离)。
- 每次 smoke 测后已还原 video-library 到干净态(0 资产/0 缺口)，未污染真实仓库。

### 未决（外部依赖，代码已就绪差真实凭据/命令）
- `grok-video15.endpoint` 仍 `REPLACE_ME`；配 `XAI_API_KEY` 即用(ApiBackend 已实现)。
- `h3.invocation.cmd` 仍 `REPLACE_ME`；配好真实 5090 H3 CLI → `video_pipeline.py run-h3` 打印工单 → 落 mp4 → `poll --auto-approve` 闭合。
- 视频参考图(i2v/r2v)路径随工单下发，ingest 时经 `--source-image/--reference` 记录。
- 真实音频闭环的 10/20 acestep 工单仍待用户给真实 5090 ACE-Step 命令(见 Round7 item②)。

## 收工提交（11:51 用户 "go next round, commit and push"）
- `git add -A && git commit` → **`bece46f`** "feat: video shot closed-loop pipeline (P0–P7) + dual-lane pre-commit gate"，35 files / +3389 / -33。
  含全部视频车道工具(backends/video_api·h3_local·mock_video、ingest/loop/coverage/fill/reconcile/qa/validate/route、video_pipeline、assemble、approve/reject、report/gen_review 扩展、test_video·test_assemble)、RUNBOOK-video.md、generators.json 视频后端重构(grok15→grok-video15 + h3，asset_kind 车道标签)、.gitignore 视频媒体、video-library 元数据壳(catalog rev0/0资产/0缺口，pristine)。
  pre-commit 双车道校验通过(bgm 46资产 OK；video 0资产 warning-only OK)。working tree 干净。
- **未 push**：仓库 `git remote -v` 为空、main 无 upstream —— 无远端可推。已向用户索要远端 URL。

## Gitea 合并推送（12:14 用户 "go merge"）
用户改主意，选「合并进现有仓库」(原 option 2)：
- Gitea `ai-film-grok` 已存在、含 801 提交（ComfyUI/film pipeline：AGENTS.md/Makefile/README.md/.omo/artifacts/ 等），与本地 8 提交**无共同历史**（unrelated）。
- 安全网：`git tag pre-merge-gitea-backup`；加 remote `origin`(clean URL) + `url.<creds>.insteadOf` 注入凭据(仅存本地 `.git/config`，非远端 URL 明文)；`git fetch origin`。
- 合并：`git merge origin/main --allow-unrelated-histories --no-edit`。**唯一冲突 `.gitignore`(add/add)**，合并为并集(保留 Gitea film-pipeline 忽略 + 本地媒体/资产忽略)；其余文件树基本不相交，自动合并。
- 合并提交 **`abb2b66`**「Merge remote-tracking branch 'origin/main'」；76 测试 + self_test 仍 PASS；关键文件(bgm-library/catalog.json、tts-evaluations/manifest.json、tools/video_pipeline.py、video-library/catalog.json、RUNBOOK-video.md)确认仍 tracked。
- 推送：`git push -u origin main` → `ccbd06d..abb2b66`，**快进、服务器 801 提交完整保留**，本地 8 提交 + 合并已上线；local 与 origin/main 同步(0 ahead)。
- 凭据经 insteadOf 存本地 `.git/config`；可 `git config --unset url.http://Redredchen01:redchen001@172.238.15.154:3000.insteadOf` 移除。
- 收工。本地 tag `pre-merge-gitea-backup` 可作回退点。

## GitHub 双远端同步（12:19 用户 "github 使用 gh ... 两边同时同步更新"）
- `gh` CLI 已装且登录 **sooneocean**(active, repo 权限)；`gh auth setup-git` 配好 git 凭据助手(走 keyring token)。
- 目标仓库 `github.com/sooneocean/ai-film-grok` 已存在（`isEmpty:false`，默认分支 `main`）。
- 加 remote `github`(clean URL, 走 gh 助手)；`git fetch github` 发现 GitHub `main`(804 提交) 与本地**有共同祖先 `ccbd06d`**(非 unrelated)——GitHub 仅比 Gitea 状态多 3 提交(`40909ef`/`105ecdd` 等，动 skills/plugin.json/CHANGELOG/内存笔记，不碰本地管线)。
- 普通三方合并：`git merge github/main` → `ac7be44`，零冲突。
- 首次 `git push github main` 被拒(远端在 fetch 后又有新提交 `a04ca73`，仅改内存 md)；`git fetch github` 重拉 → 再 `git merge github/main`(`fb7c8b6`) → 二次 push 成功。
- 最终 **local = Gitea(origin) = GitHub(github) = `fb7c8b6`**，三端同步、零冲突、均快进(无覆盖)。
- **一键双推**：`git remote set-url --add --push origin <github-url>` 后，`origin` 现有两个 push URL = [GitHub, Gitea]，故 `git push origin` 同时推两端(`--dry-run` 显示两段 "Everything up-to-date" 验证)。fetch 仍只走 Gitea(origin fetch URL)。
- 注：origin push URL 含 Gitea 明文凭据(经 insteadOf 注入)；GitHub 走 gh keyring token，不在 remote URL 落地。
- 收工：三远端(Gitea+GitHub+本地)一致；后续改动 `git push origin` 即双同步。

## 代码库工程质量 + 团队能力优化迭代 Plan（用户 12:38「分析 codebase + 提优化迭代 todo plan + 自动 commit&push」）

以 Senior Developer 角色做的**工程侧**（非产品侧）审查，成果：`docs/plans/2026-08-06-codebase-quality-todoplan.md`（互补于既有产品 plan `2026-08-06-optimization-todoplan.md`，已在后者的关系表加交叉引用行）。

### 分析方法
- 量化：`find` 统计 1091 个 .py / ~25 万行；测试 3342 个 `def test_`（398 文件）；CI `ci.yml` 真接 push/PR。
- 3 路并行 Explore 子代理：架构/可维护性、测试与门禁、工具链/CI/团队工作流。
- 对关键论断做「信任但验证」grep 复核。

### 核心发现（已核实）
- **门禁信任缺口(P0)**：`.githooks/pre-push`（secret scan + release gate）存在，但 `git config core.hooksPath` 未设 → 本地 push 不触发；AGENTS 第 11 步「pre-push 默认 light」是空头承诺。secret scan 实际只靠 CI 兜底。
- **规则散文化**：`composition_anti_hijack.py` 是唯一定义的反劫持闸（含 `AIFILM_SKIP_ANTI_HIJACK`），但 `volumedetect`/`probe_native_audio_mean_volume` 在 **8 个文件**复制，mean/音量仍被多处用作 promote 信号，绕过闸。
- **单一真相违规**：`semantic_index.py:127` 自写 `_read_json_source` 违反 AGENTS 第 82 条；retry/backoff 散落 ≥14 处无共享工具。
- **循环导入 hack**：`edit_policy_heat ↔ edit_policy` 靠 `sys.modules.get(...)` 运行时探测打破。
- **hotpath marker 失效**：`@pytest.mark.hotpath` 在代码中 **0 处实际使用**（仅 CHANGELOG 提及），`make test-hotpath` 选 0 测试。
- **巨石**：edit_policy_heat 4034 / film_spec 3156 / render_final 2869 / export_composition 2804 / workflow_pack 2177 / h3_fill_idle 2131 行。
- **文档负债**：memory 83 + references 170(81 lessons) ≈ 334 篇，新人无检索负担。
- **lint**：ruff 忽略 13 条（含 E501 长行）。

### Plan 结构（T0–T5）
- T0 门禁真相(P0) · T1 规则机读化(anti_hijack 唯一通道 + IRON 机器化) · T2 去重/单一真相 · T3 巨石风险驱动 peel(含消 sys.modules hack) · T4 测试纪律(hotpath 落地/slow 精度/gate 覆盖) · T5 团队能力 uplift(CONTRIBUTING/文档治理/REVIEW_CHECKLIST/lint 预算/AGENTS 修正)。
- 用户核心诉求「资深开发者指导 + 代码质量把控」落地为 **T5.3 PR Review Checklist + `make review` 概念 + 复杂度预算**。

### 交付与收尾
- 写 plan 文档 + 交叉引用 + 本 memory 注记；按用户要求 `git commit`(英文) + `git push origin main`（双远端 Gitea+GitHub）。
- 注意：working tree 仅 `.workbuddy-ai/memory/2026-08-06.md` 为 M；新增 plan 为 untracked。提交聚焦这两类 + 交叉引用改动，避免夹带无关文件。

## 第二趟「推进到最后 完整修复」(Senior Developer · 12:38→收工)

用户二次指令「推进到最后 完整修复」。实测关键发现（信任但验证）：

- **双 checkout 分叉**：本机 `/Users/dex/.grok/ai-film-grok`（git 根，本会话工作树）与 `/Users/dex/.grok/plugins/ai-film-grok`（插件加载处，`~/.grok/skills/ai-film-grok` symlink 指向它）是两个**分享历史但已分叉**的 git 仓库。两远端(origin=Gitea+GitHub / github / gitea / gitea-aidev)指向不同 checkout。
- **并行 agent 已落地 superset**：push 前 `git fetch --all` 发现 origin/main 已前进到 `922caf5`(2.39.90, "engineering quality uplift + H3 family apply")，覆盖了本会话本要做的：
  - `scripts/secret_scan.py`（CI 已接 secret-scan job，非 `tools/`）
  - `core.media_ops.parse_mean_volume_db`（hub/compose/h3_ship_native/h3_workflow 收敛）
  - `util/retry.py`（共享 backoff）
  - `docs/CONTRIBUTING.md` + `docs/REVIEW_CHECKLIST.md`
  - CI hotpath job（`pytest -m "hotpath and not slow"`）
- **我的 f82dfb9 因此冗余且会冲突**（重复 parse_mean_volume / 第二个 secret_scan / 与远端同改 ci.yml+AGENTS+CHANGELOG）。处置：`git reset --hard origin/main` 丢弃 f82dfb9，仅保留**独有**修正，以 `0b56c58`(2.39.91) 落地：
  1. AGENTS.md 纠错：源码 checkout 路径误写 + 「pre-push 默认 light」伪承诺→声明「CI 是唯一真实门禁」+ fetch 防领先；标注双 checkout 分叉风险。
  2. 3 个 fail-closed gate 套件(`test_bulk_preflight_hard_gate`/`test_strict_gate_paths`/`test_production_gates`)补 `pytest.mark.hotpath`（远程 2.39.90 未打标，CI hotpath job 现覆盖它们）。
  3. plan doc 第 10 节记录双 checkout 分叉 + 选定 canonical 行动项。
- 验证：ruff check/format 过；3 gate 套件 28 passed；`tools/secret_scan.py` 已弃（远程 `scripts/` 版为 canonical，正测 0 命中、正例可捕获 sk_live_/ghp_/AKIA）。`test_delivery_gates.py` 在本机 9 失败系 numpy==1.26.4 无 py3.13 wheel(managed runtime 3.13.12) 的环境性问题，非回归（CI 用 py3.11 正常）。
- 已 `git push origin main` 双远端(Gitea+GitHub)收工：`922caf5..0b56c58`。

**待用户决策**：选定唯一 canonical 仓库（建议 `plugins/ai-film-grok` 契合 AGENTS 原意），统一远端与同步流程，解决 plugins 侧未提交 h3 工作后再 `grok plugin update`。

## 第三趟「go next round」(Senior Developer · 门禁信任缺口收尾)

用户「go next round」= 执行既定下一步（圣旨协议，不重开讨论）：把 quality 门禁接进本地开发循环，收口 P0「门禁信任缺口」。

- **根因**：CI(`ci.yml`) 已含 secret-scan + hotpath job，但本地 `make check-all` → `scripts/check-all.sh` 只有 4 步(validate+ruff+doctor+pytest not-slow)，**漏掉 secret-scan 与 hotpath** → 本地绿线 ≠ CI 绿线。
- **改动(2.39.92)**：
  1. `scripts/check-all.sh`：扩展为 6 步 — 新增 **step1 secret-scan**(`scripts/secret_scan.py`) 与 **step6 hotpath**(`pytest -m "hotpath and not slow"`)，pytest 两步均带 `AIFILM_CI=1`（与 CI 行为对齐）；doctor 保持严格(无 AIFILM_CI，本机生成 runtime-lock 应全绿)。`PY="${PYTHON:-python3}"` 可覆盖解释器。
  2. `Makefile`：修正错误 header 注释（原指向 `plugins/ai-film-grok` 作 root，现标注双 checkout：git root=`ai-film-grok`、runtime mirror=`plugins/ai-film-grok`，并声明 CI 是真实门禁）；更新 `help` 文本。
- **验证**：`secret_scan.py` 本地跑 exit 0(clean)；hotpath 套件 **102 passed**(2.39.90 已存在 9 个 hotpath 模块，不止我打的 3 个)；`bash -n` 语法 OK。受 numpy==1.26.4 无 py3.13 wheel 影响，本机 pytest not-slow 有环境性失败（CI py3.11 正常），非本改动回归 —— 用隔离 venv(managed py3.13 + numpy 2.x) 验 hotpath。
- **残留(已文档化)**：本地 `check-all` 未跑 coverage 58% 阈值(CI 独有)；`core.hooksPath` 仍需 `make install-hooks` 手动启用一次。对称缺口仅剩 canonical 仓库统一(§10 主项)。
- 已 `git push origin main` 双远端(Gitea+GitHub)：`0b56c58..<new>`。

### 第三趟补遗 — 并行 agent 碰撞 + 合并收敛（同一 session 第二次）
- push 时 Gitea 已前进到 `ddc253d`（并行 agent 的「quality round-2 2.39.92」：**volume-probe 残余收敛** `core/media_ops.probe_volume_stats`/`parse_max_volume_db` + edge TTS `util.retry` 样板 + 文档 session-wrap）。GitHub 收了我的 `1f1f7d0`，Gitea 拒（non-ff）→ 双远端一度**分叉**（GitHub=1f1f7d0 / Gitea=ddc253d）。
- 关键核对：`ddc253d` 的 `scripts/check-all.sh` 仍是**旧 4 步**（没做本地门禁接线）！即两侧**互补非冗余**：我=本地门禁接线，并行=volume-probe 收敛。不能简单 `reset --hard`（会丢我任务），也不能盲目采纳（会丢对方工作）。
- 处置：**`git merge ddc253d`** → 合并提交 `b30e3ff`（双 parent）。冲突仅在 CHANGELOG（两侧各写 2.39.92 段）→ 合并为单节同时列两项；check-all.sh/Makefile 因对方==base 自动取我方 6 步版；plan doc 自动合并；volume-probe 文件干净并入。
- 结果 `git push origin main` 对 GitHub(ff 1f1f7d0→b30e3ff) + Gitea(ff ddc253d→b30e3ff) 双快进，**Gitea==GitHub==Local 不变量恢复**，无需 force。
- **可复用模式（本仓高频）**：并行 agent 常在 Gitea 抢先 landed superset/互补提交。判定冗余 vs 互补：① `git diff --stat HEAD origin/main` 看是否同文件同统计（上次 f82dfb9 vs 922caf5 是冗余→`reset --hard` 丢）；② 本次是互补→`merge` 保两边。永远先 `git fetch --all` 再 push，避免分叉。

## 第四趟「go next round」(Senior Developer · coverage 门禁 + 第三次并行碰撞)

既定 next = 上轮残余 #1：把 coverage 58% 门槛接进本地 `check-all`，彻底闭合「本地绿线 ≡ CI」。

- **改动(2.39.93→合并后 2.39.95)**：`scripts/check-all.sh` 由 6 步扩到 **7 步**，新增 **step7 coverage gate** — `coverage run` 跑 not-slow 套件 + `coverage report --fail-under=58` + 单文件 floor(`media_qa.py`45/`quality_evidence.py`80/`continuity.py`85)，完全镜像 CI `validate-core`。本地 `make check-all` 现 = validate+ruff+doctor+pytest not-slow+secret-scan+hotpath+coverage，与 CI 绿线全等。`Makefile` help/header 同步。
- **验证**：coverage 工具链(hotpath 子集上跑通：coverage run→report→json→floor heredoc)全 OK；hotpath-only 22% 触发 fail-closed 证明门槛生效。not-slow 全量 58% 由 CI(py3.11) 守，本机 numpy/py3.13 环境性失败非回归。
- **第三次并行碰撞（最严重）**：push 时 Gitea 已连跳两轮 —— `c4b62b0→8a575f4`(5 commits：T3 破 heat/policy `sys.modules` 死循环 + `SHIM_POLICY.md` + `frw_rate` retry，**版本已到 2.39.94**)，我 commit 覆盖门禁(2.39.93)后 merge 8a575f4（冲突仅 plugin.json 版本 + CHANGELOG → 解到 2.39.95 + 合并两节）；push 时 Gitea 又 force-update 到 `2dd79a3`（仅加 1 个 round-4 session-wrap md，未动 check-all/Makefile/plugin/CHANGELOG）→ 再 merge 2dd79a3（干净，仅加 md 文件）。最终 `a7290b5` 双快进收敛，Gitea==GitHub==Local。
- **固化的并行协作 SOP（本仓实测 ≤3 次/轮）**：① 改完先 `git fetch --all` 看 Gitea 是否领先；② 领先则 DM `git diff --name-only <mybase>..origin/main` 判定冗余(reset --hard) vs 互补(merge)；③ commit 自己切片 → `git merge origin/main` → 解冲突(版本取 max+1、CHANGELOG 合并两节)；④ `git fetch --all` 再 `git push origin main`，若又被 force-update 则回到①循环。coverage.json 已被 .gitignore，安全。
- 残余：仅 #2(pre-push hook 静默跳过漏扫风险) + #3(canonical 仓库统一) 待用户决策；P0 门禁信任缺口本轮彻底闭合。
