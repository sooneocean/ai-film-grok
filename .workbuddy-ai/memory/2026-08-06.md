# 2026-08-06

## Round 6 — 闭合开放缺口 + 数据自描述（A→B→C→D 全做）

侦察审计发现：
- 70 缺口 = 60 open(fill) + 10 routed_generate(generate/acep-step)。
- **60 个 open fill 缺口全部 `suggested_asset_id` 指向 approved 资产**，但 `generate_loop.do_submit` 只处理 action==generate 缺口 → 无任何自动化闭环（只能人手 fill_gap.py）。
- 10 个 generate 缺口全部缺 `duration` 字段（_build_spec 只能从 to-generate.jsonl 兜底）。

交付（实时仓库仅 dry-run，未 --apply/--fix）：
- **A** 新增 `tools/fill_open_gaps.py`：默认 --dry-run，--apply 才写盘；`eligible(gaps,cat)` 纯函数分可闭合/死路；逐条委托 fill_gap.py 闭环；--limit 限量；可重入。
- **B** `reconcile.py --fix` 按 source_gap_id 从 to-generate.jsonl 回填 generate 缺口 duration(30s)；`generate_loop._build_spec` 优先用 gap.duration；`route.py check` 加软 WARN（generate 缺 duration 不破 gate）。
- **C** `report.py` 加 open fill 可闭合/死路 + generate 缺 duration 计数；`reconcile.py` 审计加同类项。
- **D** `tools/tests/test_fill_open_gaps.py`（8 用例，纯函数+main dry-run 契约）；`self_test.py` 扩展：reconcile --fix 回填验证、注入 3 fill+1 死路断言 --apply 闭环3/留1/rev+3/use_count+3、--dry-run 不写。

验证：run_tests.py **54 测试全过**（46+8）；self_test **PASS**；实时仓库 pristine（git 现状为前几轮遗留改动，本轮仅新增 fill_open_gaps.py + tests/，命令全只读）。

应用入口（留待确认）：
1. `python3 tools/reconcile.py --fix`（回填 10 generate 缺口 duration，低危可逆）
2. `python3 tools/fill_open_gaps.py --apply`（闭环 60 fill 缺口，rev 92→152、use_count 起跳；每条有 .bak 备份）

## 已执行（用户 "GO" 后 09:52 应用）
- 已做安全备份：`/tmp/catalog.preround6.json`、`/tmp/gap-queue.preround6.jsonl`。
- `reconcile --fix`：10 generate 缺口 duration 全部回填 30.0（catalog 未改动，仅 gap-queue 存 .bak）。
- `fill_open_gaps --apply`：**60 fill 缺口全部闭环**（rev 92→152；仅 2 个资产被 60 次复用，use_count 起跳）。
- 后验：`route.py check` 0 警告 OK；`reconcile` 审计 0 缺 duration / 0 open fill  backlog；`report` 显示 60 filled + 10 routed_generate。
- 残余：10 acestep job 仍 submitted（等 5090 ACE-Step 落 wav，外部依赖）。git 改动：catalog.json + gap-queue.jsonl 已 M；.bak 被 gitignore。

## git commit
- `git commit` → `c8bd118` "feat: closed-loop pipeline automation (Round 5 + 6)"，42 files / +4025 / -143。含 Round5+6 工具、Round6 数据闭环、RUNBOOK、单测、memory。pre-commit 门禁（validate_catalog --no-sha）通过。working tree 干净。未 push（用户未要求）。

## Round 7 — 覆盖驱动的前瞻供给规划 + 统一编排入口（A→B→C→D 全做）

用户「go next」→ 重审计发现库存**严重偏科**：rnb-full 缺口 59× 仅 2 资产；ambient 库存 0（已转 10 生成缺口但待落盘）。之前只有「缺口来了才生成」的被动模式，缺少「按供需缺口前瞻排产」能力。经确认全做 A→B→C→D。

### A — 供需覆盖分析器 (`coverage.py` 新)
- 分析粒度 `(mood, stem, energy_bucket)`，`energy_bucket(e)`：`low<0.34 / mid<0.67 / high`。
- 供应 = approved 资产数；在制 = routed_generate 缺口（算半供应）；需求 = 该粒度下全部缺口数；`effective=approved+inflight`；`deficit=max(demand,target_min)-effective`（`target_min` 默认 4）。
- 分类：`STARVED`(approved=0 且 inflight=0) / `THIN`(effective<demand 或 <target_min) / `OK`。
- 纯函数 `analyze(cat,gaps,target_min)` / `priority_queue(analysis,max_total=30,per_cap=10)`（deficit 降序、按 (mood,stem) 封顶、总量封顶）。
- `--emit-generate [--apply]`：把 Top-N 落成 open `generate` 缺口 + 追加 `to-generate.jsonl` 台账（默认 dry-run；落库带 `.bak`）。缺口字段：`reason="coverage_gap"`、`dedup_key="coverage|mood|stem|bucket"`、`action="generate"`、`status="open"`、`suggested_asset_id=None`。
- `--json` 机读（top keys: target_min/rows/supply_total/demand_total/priority）。

### B — report.py 集成覆盖摘要
- `report.py` 新增 `from coverage import analyze` 调用，输出 library coverage STARVED/THIN 摘要（`try/except` 包裹，离线不影响其他行）。

### C — 统一编排入口 (`pipeline.py` 新)
- dispatcher 把 ~20 脚本收口为子命令：`check/doctor/submit/poll/run-acestep/fill-open/reconcile/coverage/one-shot/status`，各子命令经 `subprocess.run([sys.executable, HERE/script] + extra)` 委托现有已测工具（行为不变，单点入口）。
- `one-shot`：`fill-open`→`reconcile --fix`→`submit`→`run-acestep`→`poll --auto-approve`（或 `--demo` 走 MockBackend 端到端）→`route check`→`reconcile`。
- `status --json`：汇 `{catalog_revision, assets, gaps_by_status, coverage:{target_min,starved,thin}, jobs}` 为健康 blob。

### D — 测试 (`tests/test_coverage.py` 新) + self_test 加固
- `AnalyzeTest`：`test_classify_ok_thin_starved_and_inflight`（energy 0.2→low 桶）、`test_priority_queue_orders_by_deficit_and_caps`（deficit 降序 + per_cap/max_total 封顶）。
- `EmitTest`：`test_dry_run_writes_nothing`、`test_apply_writes_gaps_and_togen_with_backup`（验证 reason/action/status/None suggested + `.bak` 文件）。
- `self_test.py`：demo run 改为 `tools/pipeline.py one-shot --demo`；加 `pipeline.py check` 断言；修 `gen_filled` 按 `gap_id` 追踪（fill_gap 重写 action→"fill" 导致旧 `action=="generate"` 过滤漏掉 10 generated 缺口；`gen_ids` 在 demo 前捕获，解决「60 预填使 filled==expect(10) 断」问题）。

### 验证汇总（全绿）
- `tools/run_tests.py` → **58 测试 OK**（原 54 + test_coverage 4）。
- `tools/self_test.py` → **SELF-TEST PASS ✓**（pipeline.py one-shot --demo 端到端 + Round6 fill-open/duration 验证 + TTS 双管线分离）。
- 实时仓库只读演示：`coverage` 22 粒度 → OK 2 / THIN 19 / **STARVED 1**（`rnb/full/mid`：approved 0、inflight 0、demand 27、deficit 27）；supply 43 / demand 70 / target_min 4；`coverage --emit-generate` dry-run 优先级队列 14 条；`pipeline.py status --json`（rev 152、60 filled + 10 routed_generate、coverage starved 1/thin 19、10 acestep:submitted jobs）。
- 实时仓库 pristine：git 仅 `M tools/report.py` / `M tools/self_test.py` / `?? coverage.py` / `?? pipeline.py` / `?? tests/test_coverage.py` + memory；catalog/gap-queue 数据未动。

### 应用入口（留待确认，非被动缺口）
- `python3 tools/coverage.py --emit-generate --apply`：前瞻排产 ~14 个 generate 缺口（补齐 rnb 偏科 + 其他 THIN），需 acestep/LTX/Grok 后端真正产出音频才能闭合（外部依赖未变）。
- 本轮未 commit（用户未要求，沿用 Round6 「接上再commit」先例）。

### 已应用（用户「gO」后 10:21）
- 安全备份：`/tmp/gap-queue.preround7.jsonl`、`/tmp/to-generate.preround7.jsonl`、`/tmp/catalog.preround7.json`（catalog 未改）；工具落库另带 `.bak`。
- `coverage.py --emit-generate --apply`：**30 generate 缺口 + 30 to-generate.jsonl 条目**入队（优先级队列 deficit 累加至 max_total=30 封顶，跨 14 个 (mood,stem,bucket) 粒度；rnb/full/mid 补 4、rnb/full/high 补 2 等）。缺口字段 status=open/action=generate/reason=coverage_gap/suggested_asset_id=None，均带 dedup_key。
- `reconcile --fix`：按 source_gap_id 从 to-generate.jsonl 回填 30 个新缺口 duration=30.0（消除 route check 软 WARN）。
- 后验：`route.py check` 0 警告 OK；`reconcile` errors=0；**残余 6 WARN = 6 个 open generate 缺口「no capable backend」死路**（dark/playful/sensual/warm 中部分 mood×stem 不在任何 active 后端 capabilities 内）——真实供需能力缺口，非数据缺陷，待后端扩 capabilities 或新增后端后可填。
- 实时仓库现状：gaps 70→**100**（filled 60 / routed_generate 10 / open 30，其中 6 死路）；togen 10→40；catalog 未动（rev 152 / 46 资产）。`run_tests.py` → 58 OK。
- 未 commit（用户「gO」仅指应用 emit，未含 commit）。

## Round 7 后续 — 解锁 6 死路缺口 + acestep 真实命令推进（用户「推进全部」10:28）

### 根因定位（item ①）
- 6 个 no-capable-backend 死路缺口全部是 `stem_profile="pulse"`（energy 0.8 → high 桶）：dark/pulse×2、playful/pulse×2、rnb/pulse×2。
- 原因：active 后端 acestep/ltx23 的 `capabilities.stem_profiles` 只声明 [pad, full, thin]，漏了库内真实存在的 `pulse` 茎型（coverage 按数据里的茎型分桶，故 pulse 需求被排产却无后端认领）。
- 修复：`generators.json` 给 acestep + ltx23 的 stem_profiles 补 "pulse"；`updated_at`→2026-08-06T10:30Z；invocation.cmd 占位提示改为自描述（保留 `REPLACE_ME` 守卫 + 列出 {seed}{mood}{stem}{duration}{out}{job_id} 占位）。

### 路由（generate_loop --submit，/tmp 备份）
- 加 capability 后死路=0（pulse 全部路由到 acestep，local 优先）。
- submit 同时触发管线标准 eligibility 捷径 `find_existing_candidate`：30 个 coverage_gap open 缺口中 **20 个被现有 approved 资产复用填充**（同 mood/stem/energy 匹配，避免重复生成），**10 个路由到 acestep 待生成**（6 pulse + 4 其余无匹配）。
- 结果：gaps 100→**100**（filled 60→80、routed_generate 10→20、open 30→0）；catalog rev 152→**172**（20 次 fill 各 bump）；generation-jobs 10→**20**（全 acestep/submitted）。
- reconcile 后验：errors=0 / warnings=0（死路清零）/ info=20（在制工单）。route check 0 警告。

### item ② acestep 真实命令（仍待用户提供）
- `run_acestep.py` 守卫 `"REPLACE_ME" in cmd` → 占位命令会被跳过，不执行（不会落垃圾文件）。
- `invocation.cmd` 经 `backends/acestep.py` 用 `.format(seed, mood, stem, duration, out, job_id)` 渲染；`out`=`bgm-library/pending/{job_id}.wav`（watch_dir+out_pattern）。
- 待用户提供：**5090 节点上真实 ACE-Step 调用命令**（保留上述占位变量）。配好后 `run_acestep.py --pending` 落 wav → `generate_loop.py --poll --auto-approve` 闭合 20 工单（10 ambient + 10 coverage）。
- 本次进展（capabilities + submit 数据态）已提交（commit 见对话/ git log）。
