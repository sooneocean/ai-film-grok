# 2026-08-07 · Web 控制台 P5 · 镜头绑定 manifest + 面板深化

用户 `go` 触发 P5（圣旨协议：短令立即执行既定 next，不重开讨论）。

## 探索结论（关键约束）
- `manifest.json` 是每个 film 根的生产 manifest（`core/film_io.py`：`load_manifest`/`save_manifest`/`empty_manifest`，schema v2，`clips{}`/`stills{}` 键为 `shot_id`，记录形 `{shot_id,path,sha256,provider,status}`）。
- **只有 shot 进 manifest.json**；character/voice/bgm 各自落户 `assets.json`/`film-spec.json`/`bgm-library/catalog.json`，绝不能发明进 manifest（否则 preflight/manifest-truth 不校验、与下游读取不一致）。
- `asset_picker.select_asset` 此前只写 `selection-ledger.json` + `receipts/<kind>-selection.json`，不知 manifest。

## 改动
- `scripts/asset_picker.py`：新增 `_best_shot_candidate`（从 review_queue 取最佳候选，失败软降级）+ `_bind_shot_to_manifest`（仅 kind=="shot"：复用 `core.film_io.save_manifest` 写 `clips[shot_id].status="approved"`；无 manifest 时 bootstrap 最小 manifest；返回 `manifest_binding` 进 select_asset 响应）。非 shot 不碰 manifest。冲突 409 在绑定前抛出，manifest 不被改动。
- `scripts/web/console.html`（P5 深化）：四类面板显示更丰富字段 +「已选入生产状态」徽章 + 按钮置灰；镜头按钮提示「写入 manifest.json (clips)」；选择成功 toast 反馈 manifest 落盘。
- `tests/test_asset_picker.py`（新，5 测试）：shot 绑既有 manifest 保留字段 / 缺失 bootstrap / 非 shot 不写 / stale 冲突不改 manifest / 无 review_queue 仍软绑定。
- `tests/test_web_api.py`：+1 网关测试 `test_shot_select_binds_manifest_via_gateway`（补 `import json`）。

## 质量门
- `ruff` 全绿（含 import-order 自动修复）；`pytest` 上述 5 测试 + 网关 shot 测试 → 全控制台 35 passed；`make lock-runtime`（asset_picker.py 指纹变更）+ `make doctor` 全绿（failed_checks=[]）。
- live smoke（web_api 网关，/tmp/aifilm-demo2）：shot select 200 + clips.s1.status=approved + manifest schema_version=2；voice select 200 + manifest_binding=None（未发明状态）。✅

## 备注
- scene/prop 在控制台无对应 picker（pipeline 未接好 canonical 数据源），故 P5 仅深化已有四类面板，未新增场景/道具面板。
- P5 未 commit/push（用户未要求）。P6=门禁 fail-closed 服务端强制 403 为下一 `go` 候选。
- 圣旨协议实测：用户连发 `go` 仅触发下一阶段，不重开讨论；本次因上一轮概述文档编辑需先收尾（onboarding.py characters 校验兼容 `items`/`characters` 双 key，纯防御性）。

---

## P6–P11 交付 + push（资深开发收尾）

用户「帮我推进到最后」→ 完成 P6–P11 并 push 至 origin 双远端。

### 关键改动
- `web_core.WebConsoleForbidden`（独立 403 类，非 `WebConsoleError` 子类）；`asset_picker.select_asset` 在 `collect_gates` 返回 `blocking` 时 fail-closed 拒选（403）。双网关（stdlib `post/review_ui.py` + FastAPI `web_api.py`）均映射 403。
- canonical 绑定（P7）：voice→`film-spec.json` `cast_voices`、character→`assets.json` `characters[].selected`、shot→`manifest.json` 精确候选；scene/prop 只读；`_list_voices` 始终合并 fallback 池。
- `GET /api/console-state` 聚合器（P9）+ 总览 tab（P8）；多 tab 同步、ARIA、reduced-motion、懒音频、窗口化渲染。
- 文档 `references/web-review-console.md` + PR 模板 + `console` pytest marker（P10）。

### 发布
- 单提交 `cfcd93b`，版本 `2.40.64`（rebase 起点 2.40.57→远端已跳到 2.40.63，故 bump 到 2.40.64）。
- 已 push：GitHub `4ff33d8→cfcd93b`、Gitea `5ec189b→cfcd93b`（均 fast-forward，无强推）。
- `runtime-lock.json` 每次改 scripts 后 `make lock-runtime` 重生；`make doctor` 全绿；`pytest -m console` 41 passed；ruff 全绿。

### 冲突解决要点（rebase onto origin/main）
- `review_ui.py` 上游已重构为 shim（委托 `post.review_ui`）→ 控制台路由须落在 `post/review_ui.py`；rebase 取 HEAD shim，P6/P9 逻辑并入 `post/review_ui.py`。
- `plugin.json`/`CHANGELOG.md` 取远端后 bump 版本 + 前置 feature entry（远端已有 2.40.58 占用，故用 2.40.64）。

---

## GO ALL · live smoke + CI 常驻门禁（收尾补充）

用户「GO ALL」→ 执行上次给的两个既定 next：本地 live smoke 全链路 + 把 `console` 测试接入 CI。

### Live smoke（真实 aifilm 进程）
- 写了 `/tmp/smoke_console.py`：subprocess 起 `aifilm review-ui serve`（pyenv 3.11.15 运行时，经 shim 走 `post.review_ui`），真实 socket 跑全流程。
- 全绿：/console(200·选素材)、/api/gates、/api/assets?kind=、bad kind→400、/api/console-state、/api/onboarding、select→200(rev1)、stale→409、**blocking gate→403 `硬门禁未通过，拒绝选择/批准: adult_scale`**、cross-origin→403、bad token→401、/media-lib 越界→404。
- 踩坑：① 脚本 ROOT 误算成 /tmp → 改用绝对路径 `/Users/dex/.grok/ai-film-grok`；② 复用同一 HTTP 连接导致 select 偶发 403 → 每次请求新建连接；③ blocking 用例发 `expected_revision:0` 但账本已 rev1 → 改发 1。

### CI 常驻门禁
- `.github/workflows/ci.yml` 新增独立 `console` job（`needs: validate-core`，跑 `pytest tests/ -m console`），并入 `merge-gate` needs。YAML 已用 pyyaml 校验（jobs=validate-core/hotpath/console/test-full/merge-gate）。
- 提交 `475341a`（v2.40.68），已 push 双远端（4ceb8cd→475341a，fast-forward）。
- 注：push 时远端已从 2.40.64 跳到 2.40.67（cfcd93b 已在上游，3 个 fix 叠加）；本地 main 已同步，故本次直接 fast-forward，无需 rebase。

---

## 下一步 · 把 live smoke 落库 + PR 模板勾选（v2.40.72）

用户「下一步」→ 执行 GO ALL 后给的既定 next：把 `/tmp/smoke_console.py` 落库成可复用脚本 + 在 PR 模板补「console 门禁须绿」勾选项。

### 落库
- `skills/ai-film-grok/scripts/smoke_console.py`（新建，chmod +x）：ROOT 从 `__file__` 的 `parents[3]` 反推 git 根（repo-relative，兼容 dev/插件两 checkout），LAUNCHER 同步推导。stdlib-only 驱动，spawn `aifilm review-ui serve` 真实 socket。删掉占位 `import time`。
- `Makefile`：新增 `smoke-console` target（调 `$(SKILL)/scripts/smoke_console.py`）+ `.PHONY` + help 行。
- `.github/PULL_REQUEST_TEMPLATE.md`：控制台自查段新增「CI `console` 门禁绿（或本地 `make smoke-console` 全绿）」勾选项。

### 版本
- `plugin.json` 2.40.71→2.40.72（中间 2.40.69/70/71 为 delivery honesty-rail 工作，已在远端）；`CHANGELOG.md` 前置 `[2.40.72]` entry。

### 绿线（提交前验证）
- `make smoke-console` 真实跑通 11 项全绿（200/400/409/403/401/404 全对）。
- `make doctor` → `failed_checks: []`（runtime_lock/requirements_lock 仍 ok；证明 smoke_console.py 不在 `DEFAULT_SCRIPTS` 指纹列表，无需 `make lock-runtime`）。
- `pytest -m console` 41 passed；`ruff` 干净。
- 未提交项：`.workbuddy-ai/artifacts/senior-dev-round21-overview.md` 与 `.workbuddy-ai/plans/` 为另一 persona 自动产物，不纳入本特性提交。

---

## Onboarding 重构提案（用户反馈：太笨重像写作业）

用户原话：起步像填空作业、缺少 agent 规划感；希望「输入故事 + 主角图 → 系统自动拆解」。已产出完整 TODO plan（未实现，待评审）。

### 探查结论（落地依据）
- 当前 `onboarding.py` = 固定三步向导（references→story→characters），`go` 落 `references.json`/`style-bible.json`/`intake/story/story.md`/`intake-manifest.json` 后 `advance_local`；**不写 `film-spec.json` 的 genre/heat_scale**（故门禁一进来就 403）；无图片上传、无自动拆解。
- `plan/local_llm.py` 已是 fail-closed 私有 OpenAI 兼容客户端（`draft()`/`shot_draft()`），配置 `AIFILM_LOCAL_LLM_BASE_URL`(私有/loopback /v1)+`AIFILM_LOCAL_LLM_TOKEN`，`probe()` 探活；视觉模型 `glm-4.6v-flash` 已在 `ALLOWED_MODELS`；语义 `candidate_only/human_apply_required`。→ 拆解引擎可直接复用，需新增 `decompose()`（JSON schema + 可选视觉）。
- `drama_graph.derive_graph(root, write=True)` 从 `style-bible.json`+`film-spec.json` 反推角色/场景 → 自动拆解只要写丰富 `style-bible`+`film-spec` 即接上。
- 全仓无 multipart 上传端点（需新增 `/api/upload`，loopback+白名单+越界防护）。

### 提案要点（docs/plans/2026-08-07-onboarding-auto-decompose.md）
- 交互：Brief(故事+主角图+可选氛围chips) → 「✨让 AI 拆解」(流式思考步骤) → Plan(预填可编辑卡片:角色/场景/声线/BGM，标 `source: llm/heuristic`) → 确认并启动。
- 后端：状态升 `schema_version=2`（stage/brief/plan/plan_source）；新增 `onboarding_planner.decompose()`(+`deterministic_decompose()` 启发式兜底)；`local_llm.decompose()`；`/api/upload`、`/api/onboarding/decompose`、`/api/onboarding/plan`；`go` 消费 plan 写 `film-spec.json`(genre/heat_scale)+style-bible+intake 并 `derive_graph`。
- 不变式：token/loopback/409/404、`write_json_locked`、`human_apply_required`（方案=提案非真值）全保留。
- 分阶段 P0(脚手架+upload)→P1(拆解引擎)→P2(提交)→P3(前端重写)→P4(测试/smoke)→P5(文档/版本)。

---

## Onboarding v2 实现完成 + 双远端 push（资深开发收尾）

恢复会话后完成 P3（console.html onboarding v2 重写）+ 验证 + push。

### 实现
- `console.html` 重写 onboarding 段：Brief(故事+拖拽主角图+氛围chips) → thinking 步骤流(reduced-motion 友好) → 可编辑 Plan 卡片(角色/分镜/声线/BGM，标 `source: llm/heuristic`) → 确认启动；旧三步表单保留在折叠 `details.legacy`。
- `onboarding_planner.deterministic_decompose`（启发式兜底：无 `AIFILM_LOCAL_LLM_BASE_URL` 也能拆，识别主角/配角、推断 genre/heat、规划分镜、建议声线+BGM）+ `local_llm.decompose`（配 LLM，fail-soft 回退）。
- 双网关（`web_api.py` FastAPI + `post/review_ui.py` stdlib）新增 `POST /api/upload`(magic-byte 校验+0o600)、`/api/onboarding/brief|decompose|plan`、`GET /api/file`(path-escape 安全)。
- `go` 经 `_persist_canonical_v2` 首写 `film-spec.json` genre/heat_scale（解门禁一进来 403）；落 style-bible/intake-manifest + 尝试 `derive_graph`。
- 测试：`test_onboarding_planner.py`(新) + `test_web_api`/`test_review_ui` 扩（409/403/401/404）；修复 `review_ui.do_POST` 缩进、`onboarding` os 导入。

### 绿线
- ruff 全绿；`pytest -m console` 53 passed；`make lock-runtime` + `make doctor` 全绿（core_readiness/runtime_lock/requirements_lock/film_spec_schema 均 ok）。

### 远端竞速 / 版本碰撞（重要踩坑）
- 会话期间 github 远端快速前进：2.40.83→84→85→86→87；origin(Gitea) 落后停在 2.40.83。两远端分叉。
- 工作树曾被 checkout-drift 自动同步重置到 HEAD `649b4e5`（v2.40.83），特性改动一度丢失；**幸而全部改动安全留在 `stash@{0}`**，从 stash 恢复 tracked 文件后在 649b4e5 上重做。
- 版本号多次前移：初定 2.40.80（远端已占）→ 2.40.82（远端已占 C6.4/C5.6）→ 2.40.84（github 已占 H3 dialect）→ 2.40.86（与 github `31e0714` v2.40.86 碰撞）→ 最终 **2.40.88**（严格高于 github 2.40.87 `0dc1f3b`）。
- 解法：每次 `git fetch --all` 后 `git merge github/main`，冲突仅 CHANGELOG/plugin.json/runtime-lock（用脚本读 `github/main:CHANGELOG.md` 前置我的 entry 重生；plugin.json 取我方最高版；`make lock-runtime` 重生 lock），再 push 双远端。

### 发布
- 合并提交 `9d673a7`（Merge github/main，含 onboarding v2 + github 2.40.84/85/86/87），版本 `2.40.88`。
- `git ls-remote` 确认 origin(Gitea) 与 github 的 main 均 = `9d673a7`（双远端 fast-forward 同步，无强推）。
- 未提交项：`.workbuddy-ai/plans/`（另一 persona 自动产物 scratchpad）不参与。

---

## UI Designer · 控制台 UI/UX 优化方案（独立任务）

用户要求分析并 redesign AI Film 控制台 UI/UX。关键澄清：**真实控制台 = `skills/ai-film-grok/scripts/web/console.html`（接 `web_api.py`，CI 门禁绿）**，不是 `tools/gen_review.py` 静态审阅页（那是独立"生成候选人工审阅"链路）。

### 诊断结论（核对源码）
- 功能完整、a11y 有底子（focus-visible/reduced-motion/role=tab/aria-live 都在），但**视觉是典型 AI 味**：`--accent:#a78bfa`紫+`--accent2:#34d399`青双渐变、`body` 径向辉光 blob、面板 `backdrop-filter` 毛玻璃、🌓/📷/✨/✅/🚀 emoji 图标。
- 无真正仪表盘（"状态总览"只几条计数）；资产卡单薄（裸 audio + 单 pick 按钮，无波形/指标/筛选）；筛选仅 kind chips；无空/加载态。

### 交付（artifacts/ui-console-redesign/）
- `design-proposal.md`：UX 诊断(8 项) + 信息架构(插仪表盘) + 设计系统(温暖墨底+琥珀单强调 token 替换紫青) + 界面规范 + TODO Plan(T1–T12, ~11.5 人日，纯前端、零后端改动)。
- `prototype/console.html`：可交互方向原型（双主题、Bento 仪表盘、富化资产审阅车道带波形/内联审批、确认弹窗、toast、响应式、reduced-motion）。美学走"电影制作工作室"，刻意规避 AI 味。

### 设计决策（已固化）
- **不碰后端**：所有数据已在 `/api/assets` `/api/gates` `/api/console-state` `/api/onboarding/*`；前端只改呈现，CI `console` job 继续绿。
- 用 Space Grotesk + Sora + JetBrains Mono 替换 system-ui；emoji 全换线性 SVG 图标。
- 静态审阅页(gen_review)不在本次范围，留作后续接 approve/reject 闭环时复用同一套 token。

### 实现落地（T1 + T2 + T4 已改真实 console.html）
- 用户指令：直接改真实 `console.html`，从 T1 去 AI 味 + T4 仪表盘开始，做成连动系统的宏观工作台。
- T1 去 AI 味：移除 `<canvas id="hero">` 粒子辉光 + 其 IIFE（否则 `getElementById('hero')` 为 null 会抛错中断后续脚本）；`.tab[aria-selected]`/`.pick`/`.chip.hint` 紫青 `linear-gradient` → 纯琥珀 `--accent`；`.panel` 去掉 `backdrop-filter` 毛玻璃；`body` 早已去径向辉光。
- T2 去 emoji：🌓→月亮 SVG；📷→图片 SVG；✨/✅/🚀/⛔/⚪/⠿ 全部移除——按钮纯文字、门禁/状态用 `.dot` 色点 + 文案、stepper 用 CSS 圆点(`.tk-ico` 实心填充)、thinking 步骤 `⠿`→空圆点由 CSS 填充。
- T4 宏观工作台仪表盘：**新增默认首屏「工作台」tab**（`aria-selected=true`），合并原「状态总览」(已删)。`loadDashboard()` 并行拉 `/api/console-state` + `/api/gates` + 6×`/api/assets?kind=` → 渲染：① 门禁健康 pill；② KPI Bento(总选择/已批准clips/硬门禁/起步进度)；③ 6 素材车道(已选 X / 库 Y + 占比条)；④ 最近动态 feed；⑤ 门禁健康清单。syncState 检测到账本版本变化时若工作台可见则重渲染。
- 字体：`<head>` 已加 Space Grotesk + Sora（JetBrains Mono 备用 mono）。
- 验证：`pytest -m console` 38 passed / 1 skipped（系统 python3.14 跑，managed venv 无 pytest）；HTML 内唯一断言 `"选素材" in payload` 仍满足（tab+品牌副标均保留）；后端零改动，CI `console` job 继续绿。
- 未做（留待 T3/T5–T12）：资产卡富化(波形/指标/筛选)、确认弹窗、空/加载态、静态审阅页 gen_review 接同一 token。

---

## 资深开发 · 真实操作 e2e 跑通 + 代码质量评审（SeniorDeveloper 收尾）

用户要求"跑通模拟实际操作状况的 e2e 让我这个功能实际上线"，并以资深开发身份做代码质量把控 + 团队技术拉升。

### 诊断（缺口）
- 原 `smoke_console.py`（v2.40.72 落库）是**后端契约黑盒**：只验 HTTP 状态/错误码，从不验证①线上返回的是不是 UI 设计师那版 film-studio 页面；②前端 `cardHtml()` 读的字段后端是否真返回；③用户点"选入生产"后状态面板与**规范文件**是否真被驱动。

### 改动（`skills/ai-film-grok/scripts/smoke_console.py`）
- `setup_film_root()` 额外写自包含 `assets.json`（character c1 + bible.props.p1），让角色锁定/道具选择判定在任意机器确定性通过（不依赖全局状态）。
- 新增 23 项真实操作 e2e（总 33 项全绿）：
  1. served-UI 工件 parity：断言返回字节含 `data-tab="overview/assets/review"`、`--accent:`、6 个 `data-kind`、`loadDashboard/cardHtml/pick` 函数（证明线上吐的是真那版）。
  2. dashboard 并行加载 shape parity：6 类 `GET /api/assets?kind=` 断言 `kind`/`items` 且**非空时每条带 `cardHtml` 读的字段**（GET 路径契约守护）。
  3. selection→state 循环（mirror `pick()`）：`GET console-state` 取 R0 → `POST /api/select` 带 R0 → 断言 `revision==R0+1` 且 `console-state.recent_selections` 含该选择（voice→character→prop 全程 R0→R4 连贯）。
  4. 生产绑定流：锁角色后断言响应 `canonical_binding.bound=true` 且直接读 `assets.json` 确认 `characters[].selected=true`（**控制台真驱动管线规范文件**，非仅本地账本）。
  5. 写路径硬化：`POST select kind=nope`→400、`character=ghost`→400（GET 坏 kind 检查的 POST 延伸）。

### 代码质量评审结论（docs/console-e2e-quality-review-2026-08-07.md）
- S1(Med) `console.html:855-870` `loadDashboard` 单 `Promise.all` 全有或全无→一个库坏整页白屏；改每类独立降级。
- S2(Med,已闭环 GET) 前端↔后端字段契约无守护→已加 shape parity；建议延伸 POST 响应（`res.revision/canonical_binding.bound/manifest_binding.bound`）。
- S3(Low/Med) `review_ui._send_html` 缺 CSP/X-Frame-Options→加严格 CSP + `SAMEORIGIN`。
- S4(Low) `asset_picker.console_state` 多处裸 `except: pass` 吞降级→改结构化日志。
- S6/S7 表扬：失败即拒门禁 + 哈希绑定版本冲突，团队新写端点照抄。

### 团队技术拉升（写进 review 文档 §3）
- 测试分层：单测(`pytest -m console`) ↔ 集成(client fixture) ↔ **真实 e2e(`make smoke-console`)**——仅真实层抓"服务真能起/真返回那版 artifact/真实 HTTP 契约"。
- 契约门禁：shape parity 固化为永久断言，防前后端漂移。
- Code Review 清单 + 可观测性（禁裸 except）+ 前端正确模式（a11y tab / skeleton/empty/error 三态复用）。

### 绿线 + 发布
- 合并 origin/main `c824b9e3`(v2.41.3, Film Production OS W0-W6) 无冲突（`smoke_console.py` 远端未动）；合并后重跑 **33/33 全绿**。
- ruff 干净；`pytest -m console` 77 passed。
- 提交 `abf25541`（仅 `smoke_console.py` + review 文档；`.workbuddy-ai/memory` 改动与 `artifacts/ui-console-redesign`/`.workbuddy-ai/plans` 不在本特性，未纳入）。
- 已 push 双远端（github + Gitea `c824b9e3→abf25541`，fast-forward，无强推）；local==origin==github。
- 未做（待团队）：S1 工作台局部降级、S2 POST 响应契约、S3 CSP、S4 结构化日志。

---

## 资深开发 · 控制台"点画面不会动"根因修复 + 指挥台 + 双远端 push（v2.41.5）

用户反馈：控制台"现在好像还不能用 我点画面不会动"；要求以 CTO 视角给出优化 TODO plan 并直接改到上线。

### 根因（关键发现）
- 原诊断"onboarding tab 从未 loadOnboarding()"在**远端已合并的 film-studio 重写版**(240ba374)依然成立：`loadOnboarding()` 已定义（`git show` 计数 1）但 `activateTab` 里 `if (b.dataset.tab === 'onboarding')` 计数 0 → 起步 tab 打开即死。
- 远端重写版**也**没有指挥台（无 `dash-command`/`cmd-go`/`cmd-advance`）和致命错误浮条（`fatal` 计数 0）→ 用户既看不到"指挥"入口，脚本崩了也只觉"冻屏"。
- 即：视觉重皮肤(去 AI 味 + 仪表盘 overview tab)已合，但"可指挥 + 可观测"未做。我的修复仍然必要、且不冗余。

### 冲突处理（远端竞速）
- 本地未提交改动基于旧 base(abf25541)；远端已把 console.html 结构重写为 film-studio 版。旧结构补丁无法直接套用。
- 处置：`cp` 备份 `/tmp/console_myfix_backup.html` → `git checkout -- console.html` 丢弃旧补丁 → `git merge origin/main`（fast-forward，36 文件，无冲突，working tree 仅 smoke_console.py + memory 改动未被远端触碰）→ 在**新结构**上重做 T0/T1/T2/T3。

### 改动（`skills/ai-film-grok/scripts/web/console.html`，基于 240ba374 新结构）
- T1：`activateTab` 加 `if (b.dataset.tab === 'onboarding') loadOnboarding();`（复活起步 tab）。
- T2：仪表盘 `#dashboard` 末插入 `#dash-command` 指挥台面板（刷新/开始起步/启动流水线/推进审核队列 + `cmd-log` 状态 + `dash-next` 引导）；新增 `cmdGo`(`POST /api/onboarding/go`)/`cmdAdvance`(`POST /api/advance`)/`cmdOnboarding`/`cmdLog` + 按钮接线。
- T0：全局 `error`/`unhandledrejection` → `#fatal` 底部红条（`showFatal`）。
- T3：`renderDashboard` 末尾按 `ob.completed` / `g.blocking` 写 `dash-next` 引导文案。
- 样式紧跟新 token 系统（`--accent`/`--panel`/`--danger`/`--muted`），未引入旧紫青/毛玻璃。

### 质量门（T4 验证）
- `smoke_console.py` 扩展：断言 served console 含 `dash-command`/`cmd-go`/`cmd-advance` + `onboarding` tab 懒加载；`POST /api/onboarding/go`、`POST /api/advance` 端点可达（非 404/405/500；fresh root 返回 400/409 结构化错误即算 wire 活）。ruff 干净。
- 跑通：**35 项 live 全绿**（含新增 3 项指挥台断言）；`pytest -m console` **78 passed**；JS `node --check` SYNTAX_OK。
- 版本：`plugin.json` 2.41.4→2.41.5；`CHANGELOG.md` 前置 `[2.41.5]` entry。

### 发布
- 提交（fix 含 console.html + smoke_console.py + plugin.json + CHANGELOG；docs 含 `artifacts/ui-console-redesign/` 设计提案；chore 含 memory）。
- 已 `git fetch --all` 并 push 双远端（origin Gitea + github），fast-forward，无强推。

### 备注
- `artifacts/ui-console-redesign/design-proposal.md` 是 UI Designer 给出的**更宏观视觉 overhaul 方案**(T1–T12 ~11.5 人日：去 AI 味 token / Bento 仪表盘 / 资产卡富化波形+指标+筛选+对比 / 空加载态)，本次未实现（仅采纳其 T1 去 AI 味 + T4 仪表盘思路，由远端重写版落地）。留作后续独立计划。
- `.workbuddy-ai/plans/`(swift-nebula-lovelace.md) 为另一 persona scratchpad，按惯例不参与提交。
