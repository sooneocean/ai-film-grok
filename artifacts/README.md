# artifacts/ — ops receipts (not product source)

Local canaries, drain logs, and session closeout dumps live here.

- **Not** the film production spine; agents should not treat these as IRON.
- Prefer film-root `receipts/` for per-film truth.
- Many `*canary*.json` / `*.log` patterns are gitignored (see repo `.gitignore`).
- Historical files already tracked may remain until a deliberate `git rm --cached` cleanup.

Active structure slim board: `docs/plans/2026-08-07-code-slim-consolidation-todoplan.md`.
